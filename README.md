# Simple TODO application
## Phase1:
This is a simple ToDo Application where one can track a list of to do items by creating, updating and deleting the tasks.

Phase 1 implements front end with React.  

## Phase2:
Adding back end java with Spring boot with simple static JSON inputs and connect it to front end, has only one end point and return the static json for the todo's list. 

